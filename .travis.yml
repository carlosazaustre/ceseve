---
language: "node_js"
node_js:
  - "10"
install: npm install
script:
  - npm test
jobs:
  include:
    deploy:
      provider: npm
      email: "$EMAIL_ADDRESS"
      api_key: "$API_KEY"
      on:
        branch: master
